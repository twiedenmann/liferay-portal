definition {

	macro createRoutine {
		Click(locator1 = "Button#PLUS");

		if (isSet(routineName)) {
			PortletEntry.inputName(name = ${routineName});
		}

		Button.clickSave();
	}

	macro editRoutine {
		if (isSet(newName)) {
			PortletEntry.inputName(name = ${newName});
		}

		Button.clickSave();
	}

}