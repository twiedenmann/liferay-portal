/**
 * SPDX-FileCopyrightText: (c) 2023 Liferay, Inc. https://liferay.com
 * SPDX-License-Identifier: LGPL-2.1-or-later OR LicenseRef-Liferay-DXP-EULA-2.0.0-2023-06
 */

package com.liferay.source.formatter.dependencies.upgrade;

/**
 * @author Michael Cavalcanti
 */
public class UpgradeJavaIndexerCheck {

	public void method(Indexer indexer) {
		List<IndexerPostProcessor> indexerPostProcessors =
			_indexerPostProcessorsServiceTrackerMap.getService(
				indexer.getClassName());

		Indexer indexer = portlet.getIndexerInstances();

		List<Indexer> indexers = portlet.getIndexerInstances();

		LinkedHashMap<IndexerRequest, Indexer> _indexerRequests =
			new LinkedHashMap<>();
	}

	public void method2(
		ServiceReference<Indexer> serviceReference, Indexer indexer) {

		LinkedHashMap<Indexer, IndexerRequest> _indexerRequests =
			new LinkedHashMap<>();

		LinkedHashMap<Indexer, Indexer> _indexerRequests =
			new LinkedHashMap<>();
	}

}