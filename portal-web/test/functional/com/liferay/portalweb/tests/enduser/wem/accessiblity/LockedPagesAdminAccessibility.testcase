@component-name = "portal-wcm"
definition {

	property ci.retries.disabled = "true";
	property custom.properties = "feature.flag.LPS-180328=true";
	property portal.release = "true";
	property portal.upstream = "true";
	property testray.component.names = "Echo Accessibility";
	property testray.main.component.name = "Content Pages";

	setUp {
		task ("Set up virtual instance") {
			TestCase.setUpPortalInstance();
		}

		task ("Sign in") {
			User.firstLoginPG();
		}

		task ("Add a site") {
			HeadlessSite.addSite(siteName = "Test Site Name");
		}
	}

	@description = "This is a test for LPS-194238. The Locked Pages admin should comply with WCAG 2.1 AA standard with the filter menu and sort menu opened."
	@priority = 4
	test ViewAccessibilityOfFilterAndOrder {
		task ("Given a locked content page") {
			JSONLayout.addPublicLayout(
				groupName = "Test Site Name",
				layoutName = "Test Page Name",
				type = "content");

			ContentPagesNavigator.openEditContentPage(
				pageName = "Test Page Name",
				siteName = "Test Site Name");

			PageEditor.addFragment(
				collectionName = "Basic Components",
				fragmentName = "Button");
		}

		task ("When a user accesses to the Locked Pages admin in a new tab") {
			var baseURL = PropsUtil.get("portal.url");

			Open.openInTheNewTab(value1 = "${baseURL}/web/test-site-name");

			ProductMenu.gotoPortlet(
				category = "Configuration",
				portlet = "Locked Pages");
		}

		task ("Then the filter and order should have correct accessibility") {
			AssertAccessible();

			Click.waitForMenuToggleJSClick(locator1 = "ManagementBar#FILTER_AND_ORDER");

			AssertElementAccessible(locator1 = "//div[contains(@class,'show') and contains(@class,'dropdown')]//li[contains(@class,'dropdown-subheader')][normalize-space()='Filter by Type']");

			AssertElementAccessible(locator1 = "//div[contains(@class,'show')][contains(@class,'menu')][not(contains(@class,'product-menu'))]//*[normalize-space(text())='Content Page']");

			AssertElementAccessible(locator1 = "//div[contains(@class,'show')][contains(@class,'menu')][not(contains(@class,'product-menu'))]//*[normalize-space(text())='Collection Page']");
		}
	}

	@description = "This is a test for LPS-182024. The Locked Pages admin should comply with WCAG 2.1 AA standard."
	@priority = 4
	test ViewAccessibilityOfLockedPages {
		task ("Given a user doesn't have locked pages") {
			SetWindowSize.maximizeWindow();

			ApplicationsMenu.gotoSite(site = "Test Site Name");
		}

		task ("When a user accesses to the Locked Pages admin") {
			ProductMenu.gotoPortlet(
				category = "Configuration",
				portlet = "Locked Pages");
		}

		task ("Then the Locked Pages admin should have correct accessibility") {
			AssertAccessible();
		}

		task ("Given a user has a locked pages") {
			JSONLayout.addPublicLayout(
				groupName = "Test Site Name",
				layoutName = "Test Page Name",
				type = "content");

			ContentPagesNavigator.openEditContentPage(
				pageName = "Test Page Name",
				siteName = "Test Site Name");

			PageEditor.addFragment(
				collectionName = "Basic Components",
				fragmentName = "Button");
		}

		task ("When a user accesses to the Locked Pages admin in a new tab") {
			var baseURL = PropsUtil.get("portal.url");

			Open.openInTheNewTab(value1 = "${baseURL}/web/test-site-name");

			ProductMenu.gotoPortlet(
				category = "Configuration",
				portlet = "Locked Pages");
		}

		task ("Then the Locked Pages admin should have correct accessibility") {
			AssertAccessible();
		}

		task ("When the user unlocks the locked page") {
			LexiconTable.clickEllipsisItem(
				item = "Unlock",
				tableEntry = "Test Page Name");
		}

		task ("Then the Unlock Pages modal should have correct accessibility") {
			AssertAccessible();
		}
	}

	@description = "This is a test for LPS-182060. The Locked Pages admin should comply with WCAG 2.1 AA standard."
	@priority = 4
	test ViewAccessibilityOfLockedPagesUnderInstanceSettings {
		task ("Given a user accesses to the Instance Settings") {
			ApplicationsMenu.gotoPortlet(
				category = "Configuration",
				panel = "Control Panel",
				portlet = "Instance Settings");
		}

		task ("When the user accesses to the Locked Pages settings") {
			SystemSettings.gotoConfiguration(
				configurationCategory = "Pages",
				configurationName = "Locked Pages",
				configurationScope = "Virtual Instance Scope");
		}

		task ("Then the Locked Pages should have correct accessibility") {
			AssertAccessible();
		}
	}

}